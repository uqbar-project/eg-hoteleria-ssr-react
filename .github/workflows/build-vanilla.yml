name: Build Vanilla

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter vanilla build:public
      - run: pnpm vitest run --coverage vanilla/__tests__/
      - uses: codecov/codecov-action@v6
        with:
          files: coverage/lcov.info
          flags: vanilla
          fail_ci_if_error: true
